[project]
name = "vectorizer-ai"
version = "0.1.0"
description = "Python SDK for Vectorizer (768d, workspace-isolated)"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
